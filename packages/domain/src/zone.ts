export type ZonePreset = {
  color: string
  maxPercent: null | number
  minPercent: number
  name: string
  description?: string
}

export type ZoneModelInfo = {
  description: string
  label: string
  referenceUrl?: string
}
