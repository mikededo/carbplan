{
  "name": "@carbplan/root",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "packageManager": "bun@1.3.10",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "preview": "turbo run preview",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "2.8.18"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
